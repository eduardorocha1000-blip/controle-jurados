<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <!-- Cabeçalho -->
    <header class="header-custom">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-auto">
                    <img src="/images/logo-tjsc.png" alt="Logo TJSC" class="header-logo">
                </div>
                <div class="col">
                    <h1 class="header-title">VARA ÚNICA DA COMARCA DE CAPIVARI DE BAIXO</h1>
                    <p class="header-subtitle">by Eduardo Motta Rocha da Silva</p>
                </div>
                <div class="col-auto">
                    <% if (typeof user !== 'undefined' && user) { %>
                    <div class="dropdown">
                        <button class="btn btn-outline-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown">
                            👤 <%= user.nome %>
                        </button>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="/perfil">⚙️ Perfil</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <form method="POST" action="/login/logout" class="d-inline">
                                    <button type="submit" class="dropdown-item">🚪 Sair</button>
                                </form>
                            </li>
                        </ul>
                    </div>
                    <% } %>
                </div>
            </div>
        </div>
    </header>

    <div class="main-container">
        <!-- Sidebar -->
        <nav class="sidebar">
                <div class="p-3">
                    <ul class="nav flex-column">
            <li class="nav-item">
                <a class="nav-link" href="/dashboard">
                    📊 Dashboard
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/usuarios">
                    👥 Usuários
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/juizes">
                    ⚖️ Juízes
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/instituicoes">
                    🏢 Instituições
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/jurados">
                    👥 Jurados
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/indicacoes">
                    📋 Indicações
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/sorteios">
                    🎲 Sorteios
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/ultimo-conselho">🙋 Último Conselho</a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" href="/editais">
                    📄 Editais
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/emails">
                    📧 E-mails
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/relatorios">
                    📊 Relatórios
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/backup">
                    💾 Backup
                </a>
            </li>
            <li class="nav-item mt-3">
                <a class="nav-link" href="/logout">
                    🚪 Sair
                </a>
            </li>
                    </ul>
                </div>
        </nav>

        <!-- Conteúdo Principal -->
        <main class="main-content">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h2 class="mb-0">Visualizar Edital</h2>
                <div>
                    <a href="/editais" class="btn btn-back me-2" title="Voltar">⬅️</a>
                    <a href="/editais/<%= edital.id %>/editar" class="btn btn-edit" title="Editar">✏️</a>
                </div>
            </div>

            <!-- Flash Messages -->
            <% if (typeof flash !== 'undefined' && flash.success) { %>
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    <%= flash.success %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            <% } %>
            
            <% if (typeof flash !== 'undefined' && flash.error) { %>
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    <%= flash.error %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            <% } %>

            <!-- Edital Details -->
            <div class="row">
                <div class="col-md-8">
                    <div class="card">
                        <div class="card-header">
                            <h5 class="mb-0">📋 Informações do Edital</h5>
                        </div>
                        <div class="card-body">
                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <strong>Número:</strong>
                                    <p class="text-muted"><%= edital.numero %></p>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <strong>Ano de Referência:</strong>
                                    <p class="text-muted"><%= edital.ano_referencia %></p>
                                </div>
                                <div class="col-md-12 mb-3">
                                    <strong>Título:</strong>
                                    <p class="text-muted"><%= edital.titulo %></p>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <strong>Status:</strong>
                                    <p>
                                        <% 
                                        let badgeClass = 'bg-secondary';
                                        if (edital.status === 'Rascunho') badgeClass = 'bg-warning';
                                        else if (edital.status === 'Publicado') badgeClass = 'bg-success';
                                        else if (edital.status === 'Vencido') badgeClass = 'bg-danger';
                                        %>
                                        <span class="badge <%= badgeClass %>">
                                            <%= edital.status || 'Rascunho' %>
                                        </span>
                                    </p>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <strong>Juiz Responsável:</strong>
                                    <p class="text-muted"><%= edital.juiz_nome || 'Não definido' %></p>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <strong>Data de Publicação Prevista:</strong>
                                    <p class="text-muted">
                                        <%= edital.data_publicacao_prevista ? new Date(edital.data_publicacao_prevista + 'T00:00:00').toLocaleDateString('pt-BR') : 'Não definida' %>
                                    </p>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <strong>Data de Publicação Real:</strong>
                                    <p class="text-muted">
                                        <%= edital.data_publicacao_real ? new Date(edital.data_publicacao_real + 'T00:00:00').toLocaleDateString('pt-BR') : 'Não definida' %>
                                    </p>
                                </div>
                                <% if (edital.observacoes) { %>
                                <div class="col-md-12 mb-3">
                                    <strong>Observações:</strong>
                                    <p class="text-muted"><%= edital.observacoes %></p>
                                </div>
                                <% } %>
                            </div>
                        </div>
                    </div>
                </div>
                
                <div class="col-md-4">
                    <div class="card">
                        <div class="card-header">
                            <h5 class="mb-0">📄 Conteúdo RTF</h5>
                        </div>
                        <div class="card-body">
                            <% if (edital.corpo_rtf) { %>
                                <div class="bg-light p-3 rounded" style="max-height: 300px; overflow-y: auto;">
                                    <pre style="white-space: pre-wrap; font-size: 0.9em;"><%= edital.corpo_rtf %></pre>
                                </div>
                            <% } else { %>
                                <p class="text-muted">Nenhum conteúdo RTF definido.</p>
                            <% } %>
                        </div>
                    </div>
                    
                    <div class="card mt-3">
                        <div class="card-header">
                            <h5 class="card-title mb-0">Ações Rápidas</h5>
                        </div>
                        <div class="card-body">
                            <div class="d-grid gap-2">
                                <a href="/editais/<%= edital.id %>/editar" class="btn btn-warning" title="Editar">✏️</a>
                                <button class="btn btn-info" onclick="gerarRTF(<%= edital.id %>)" title="Gerar RTF">📄</button>
                                <button class="btn btn-primary" onclick="gerarPDF(<%= edital.id %>)" title="Gerar PDF">📋</button>
                                <button class="btn btn-danger" onclick="excluirEdital(<%= edital.id %>)" title="Excluir">🗑️</button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </main>
    </div>

    <!-- Rodapé -->
    <footer class="footer-custom">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <p class="mb-0">Total de Jurados: <span id="total-jurados">-</span></p>
                </div>
                <div class="col-md-6 text-md-end">
                    <p class="mb-0">Total de Instituições: <span id="total-instituicoes">-</span></p>
                </div>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            fetch('/api/stats')
                .then(response => response.json())
                .then(data => {
                    const tj = document.getElementById('total-jurados');
                    const ti = document.getElementById('total-instituicoes');
                    if (tj) tj.textContent = data.totalJurados || 0;
                    if (ti) ti.textContent = data.totalInstituicoes || 0;
                })
                .catch(err => console.error('Erro ao buscar estatísticas:', err));
        });
    </script>
    <script>
        function gerarRTF(id) {
            window.location.href = `/editais/${id}/gerar-rtf`;
        }

        function gerarPDF(id) {
            window.location.href = `/editais/${id}/pdf`;
        }

        function excluirEdital(id) {
            fetch(`/editais/${id}`, {
                method: 'DELETE'
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    alert('Edital excluído com sucesso!');
                    window.location.href = '/editais';
                } else {
                    alert('Erro ao excluir edital: ' + data.message);
                }
            })
            .catch(error => {
                alert('Erro ao excluir edital: ' + error.message);
            });
        }
    </script>
</body>
</html>
