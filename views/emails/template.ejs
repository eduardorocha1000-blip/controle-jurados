<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <!-- Cabeçalho -->
    <header class="header-custom">
        <div class="container-fluid">
            <div class="d-flex justify-content-between align-items-center">
                <div class="d-flex align-items-center">
                    <img src="/images/logo-tjsc.png" alt="Logo TJSC" class="header-logo me-3">
                    <div>
                        <h1 class="header-title">Controle de Jurados</h1>
                        <p class="header-subtitle">Comarca de Capivari de Baixo</p>
                    </div>
                </div>
                <% if (typeof user !== 'undefined' && user) { %>
                <div class="dropdown">
                    <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-bs-toggle="dropdown" aria-expanded="false">
                        👤 <%= user.nome %>
                    </button>
                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="dropdownMenuButton">
                        <li><a class="dropdown-item" href="/perfil">⚙️ Perfil</a></li>
                        <% if (user.perfil === 'Administrador') { %>
                        <li><a class="dropdown-item" href="/usuarios">👥 Usuários</a></li>
                        <% } %>
                        <li><hr class="dropdown-divider"></li>
                        <li>
                            <form action="/login/logout" method="POST" class="d-inline">
                                <button type="submit" class="dropdown-item">🚪 Sair</button>
                            </form>
                        </li>
                    </ul>
                </div>
                <% } %>
            </div>
        </div>
    </header>

    <div class="main-container">
        <% if (typeof user !== 'undefined' && user) { %>
        <!-- Sidebar -->
        <nav class="sidebar">
            <div class="p-3">
                <ul class="nav flex-column">
                    <li class="nav-item">
                        <a class="nav-link" href="/dashboard">📊 Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/juizes">⚖️ Juízes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/instituicoes">🏢 Instituições</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/jurados">👥 Jurados</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/indicacoes">📋 Indicações</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/sorteios">🎲 Sorteios</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/cedulas">🎫 Cédulas</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/editais">📄 Editais</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/emails">📧 E-mails</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/relatorios">📈 Relatórios</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/backup">💾 Backup</a>
                    </li>
                </ul>
            </div>
        </nav>
        <% } %>

        <!-- Conteúdo Principal -->
        <main class="main-content p-4">
            <% if (typeof messages !== 'undefined' && messages) { %>
                <% if (messages.success && messages.success.length > 0) { %>
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    ✅ <%= messages.success[0] %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
                <% } %>
                <% if (messages.error && messages.error.length > 0) { %>
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    ❌ <%= messages.error[0] %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
                <% } %>
            <% } %>

            <% if (typeof error !== 'undefined' && error) { %>
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                ❌ <%= error %>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
            <% } %>

            <div class="d-flex justify-content-between align-items-center mb-4">
                <h2>Editar Template de E-mail - <%= tipo === 'intimacao' ? 'Intimação' : 'Confirmação' %></h2>
            </div>

            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">
                        <i class="fas fa-file-alt btn-icon"></i>Template de <%= tipo === 'intimacao' ? 'Intimação' : 'Confirmação' %>
                    </h5>
                </div>
                <div class="card-body">
                    <form id="templateForm" method="POST" action="/emails/template/<%= tipo %>">
                        <div class="mb-3">
                            <label for="assunto" class="form-label">Assunto <span class="text-danger">*</span></label>
                            <input type="text" class="form-control" id="assunto" name="assunto" value="<%= typeof template !== 'undefined' && template ? template.assunto : '' %>" required data-keep-lowercase="true" style="text-transform: none !important;">
                            <div class="form-text">
                                Assunto do e-mail que será enviado. 
                                <small class="text-muted">💡 Você pode usar variáveis como <code>{ANO}</code>, <code>{INSTITUICAO}</code>, etc.</small>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label for="corpo_html" class="form-label">Corpo HTML <span class="text-danger">*</span></label>
                            <textarea class="form-control" id="corpo_html" name="corpo_html" rows="20" required data-keep-lowercase="true" style="text-transform: none !important;"><%- typeof template !== 'undefined' && template && template.corpo_html ? template.corpo_html : '' %></textarea>
                            <div class="form-text">
                                <strong>Variáveis disponíveis:</strong> 
                                <code>{NOME}</code> (nome do contato), 
                                <code>{INSTITUICAO}</code> (nome da instituição), 
                                <code>{ANO}</code> (ano de referência), 
                                <code>{QUANTIDADE}</code> (quantidade solicitada), 
                                <code>{PRAZO}</code> (prazo para envio), 
                                <code>{LINK_UPLOAD}</code> (link para upload), 
                                <code>{JUIZ_TITULAR}</code> (nome do juiz titular).
                                <br>
                                <small class="text-muted">💡 Use <code>{NOME}</code>, <code>{INSTITUICAO}</code>, etc. no HTML. Os valores serão substituídos automaticamente ao enviar o e-mail.</small>
                            </div>
                        </div>

                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-edit btn-sm" title="Salvar">💾</button>
                            <a href="/emails" class="btn btn-back btn-sm" title="Voltar">⬅️</a>
                        </div>
                    </form>
                </div>
            </div>
        </main>
    </div>

    <!-- Rodapé -->
    <footer class="footer-custom">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <p class="mb-0">Total de Jurados: <span id="total-jurados">-</span></p>
                </div>
                <div class="col-md-6 text-md-end">
                    <p class="mb-0">Total de Instituições: <span id="total-instituicoes">-</span></p>
                </div>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Inicializar quando DOM estiver pronto
        document.addEventListener('DOMContentLoaded', function() {
            // Proteger campos de conversão para maiúsculas
            const campos = ['assunto', 'corpo_html'];
            campos.forEach(function(campoId) {
                const campo = document.getElementById(campoId);
                if (campo) {
                    // Garantir que o campo não seja convertido
                    campo.setAttribute('data-keep-lowercase', 'true');
                    campo.style.textTransform = 'none';
                    
                    // Adicionar listener que preserva o texto original
                    campo.addEventListener('input', function(e) {
                        const originalValue = this.value;
                        // Se o valor foi convertido para maiúsculas, restaurar
                        setTimeout(() => {
                            if (this.value !== originalValue && this.value === originalValue.toUpperCase()) {
                                this.value = originalValue;
                            }
                        }, 0);
                    });
                }
            });
            
            // Feedback visual para formulário
            const templateForm = document.getElementById('templateForm');
            if (templateForm) {
                templateForm.addEventListener('submit', function(e) {
                    const btn = this.querySelector('button[type="submit"]');
                    btn.innerHTML = '<i class="fas fa-spinner fa-spin btn-icon"></i>Salvando...';
                    btn.disabled = true;
                });
            }
            
            // Carregar totais no rodapé
            fetch('/api/stats')
                .then(response => response.json())
                .then(data => {
                    document.getElementById('total-jurados').textContent = data.totalJurados || 0;
                    document.getElementById('total-instituicoes').textContent = data.totalInstituicoes || 0;
                })
                .catch(error => console.error('Erro ao carregar estatísticas:', error));
        });
    </script>
</body>
</html>

