<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css?v=<%= Date.now() %>">
</head>
<body>
    <!-- Cabeçalho -->
    <header class="header-custom">
        <div class="container-fluid">
            <div class="d-flex justify-content-between align-items-center">
                <div class="d-flex align-items-center">
                    <img src="/images/logo-tjsc.png" alt="Logo TJSC" class="header-logo me-3">
                    <div>
                        <h1 class="header-title">Controle de Jurados</h1>
                        <p class="header-subtitle">Comarca de Capivari de Baixo</p>
                    </div>
                </div>
                <% if (typeof user !== 'undefined' && user) { %>
                <div class="dropdown">
                    <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-bs-toggle="dropdown" aria-expanded="false">
                        👤 <%= user.nome %>
                    </button>
                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="dropdownMenuButton">
                        <li><a class="dropdown-item" href="/perfil">⚙️ Perfil</a></li>
                        <% if (user.perfil === 'Administrador') { %>
                        <li><a class="dropdown-item" href="/usuarios">👥 Usuários</a></li>
                        <% } %>
                        <li><hr class="dropdown-divider"></li>
                        <li>
                            <form method="POST" action="/login/logout" class="d-inline">
                                <button type="submit" class="dropdown-item">🚪 Sair</button>
                            </form>
                        </li>
                    </ul>
                </div>
                <% } %>
            </div>
        </div>
    </header>

    <div class="container-fluid main-content">
        <div class="row">
            <!-- Sidebar -->
            <nav class="col-md-3 col-lg-2 sidebar p-0">
                <% if (typeof user !== 'undefined' && user) { %>
                <div class="sidebar-sticky">
                    <ul class="nav flex-column">
                        <li class="nav-item">
                            <a class="nav-link" href="/dashboard">
                                📊 Dashboard
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/juizes">
                                ⚖️ Juízes
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/instituicoes">
                                🏢 Instituições
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/jurados">
                                👥 Jurados
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link active" href="/indicacoes">
                                📋 Indicações
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/sorteios">
                                🎲 Sorteios
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/editais">
                                📄 Editais
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/relatorios">
                                📊 Relatórios
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/emails">
                                📧 E-mails
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/backup">
                                💾 Backup
                            </a>
                        </li>
                    </ul>
                </div>
                <% } %>

                <!-- Conteúdo Principal -->
                <main class="main-content">
                    <% if (typeof messages !== 'undefined' && messages) { %>
                        <% if (messages.success && messages.success.length > 0) { %>
                        <div class="alert alert-success alert-dismissible fade show" role="alert">
                            ✅ <%= messages.success[0] %>
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                        <% } %>
                        <% if (messages.error && messages.error.length > 0) { %>
                        <div class="alert alert-danger alert-dismissible fade show" role="alert">
                            ❌ <%= messages.error[0] %>
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                        <% } %>
                    <% } %>

                    <% if (typeof error !== 'undefined' && error) { %>
                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                        <i class="fas fa-exclamation-triangle btn-icon"></i><%= error %>
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                    <% } %>

                    <div class="container-fluid">
                        <!-- Cabeçalho -->
                        <div class="row mb-4">
                            <div class="col-12">
                                <div class="d-flex justify-content-between align-items-center">
                                    <div>
                                        <h2><%= typeof indicacao !== 'undefined' && indicacao && indicacao.id ? 'Editar Indicação' : 'Nova Indicação' %></h2>
                                        <p class="text-muted mb-0">Cadastro de indicação de instituição</p>
                                    </div>
                                    <div>
                                        <a href="/indicacoes" class="btn btn-secondary">
                                            ⬅️
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Formulário -->
                        <div class="row">
                            <div class="col-lg-8">
                                <div class="card">
                                    <div class="card-header">
                                        <h5 class="card-title mb-0">Dados da Indicação</h5>
                                    </div>
                                    <div class="card-body">
                                        <form method="POST" action="<%= typeof action !== 'undefined' ? action : '/indicacoes' %>">
                                            <% if (typeof indicacao !== 'undefined' && indicacao && indicacao.id) { %>
                                            <input type="hidden" name="_method" value="PUT">
                                            <% } %>
                                            
                                            <div class="row">
                                                <div class="col-md-6 mb-3">
                                                    <label for="instituicao_id" class="form-label">Instituição *</label>
                                                    <select class="form-select" id="instituicao_id" name="instituicao_id" required>
                                                        <option value="">Selecione...</option>
                                                        <% if (typeof instituicoes !== 'undefined' && instituicoes && instituicoes.length > 0) { %>
                                                            <% instituicoes.forEach(instituicao => { %>
                                                                <option value="<%= instituicao.id %>" 
                                                                        <%= typeof indicacao !== 'undefined' && indicacao && indicacao.instituicao_id == instituicao.id ? 'selected' : '' %>>
                                                                    <%= instituicao.nome %>
                                                                </option>
                                                            <% }) %>
                                                        <% } else { %>
                                                            <option value="" disabled>Nenhuma instituição cadastrada</option>
                                                        <% } %>
                                                    </select>
                                                </div>
                                                
                                                <div class="col-md-6 mb-3">
                                                    <label for="ano_referencia" class="form-label">Ano de Referência *</label>
                                                    <input type="number" class="form-control" id="ano_referencia" name="ano_referencia" 
                                                           value="<%= typeof indicacao !== 'undefined' && indicacao && indicacao.ano_referencia ? indicacao.ano_referencia : new Date().getFullYear() %>" 
                                                           min="2020" max="2030" required>
                                                </div>
                                            </div>

                                            <div class="row">
                                                <div class="col-md-6 mb-3">
                                                    <label for="status" class="form-label">Status *</label>
                                                    <select class="form-select" id="status" name="status" required>
                                                        <option value="pendente" <%= typeof indicacao !== 'undefined' && indicacao && indicacao.status === 'pendente' ? 'selected' : '' %>>Pendente</option>
                                                        <option value="recebida" <%= typeof indicacao !== 'undefined' && indicacao && indicacao.status === 'recebida' ? 'selected' : '' %>>Recebida</option>
                                                        <option value="validada" <%= typeof indicacao !== 'undefined' && indicacao && indicacao.status === 'validada' ? 'selected' : '' %>>Validada</option>
                                                    </select>
                                                </div>
                                                
                                                <div class="col-md-6 mb-3">
                                                    <label for="arquivo_lista" class="form-label">Arquivo da Lista</label>
                                                    <input type="text" class="form-control" id="arquivo_lista" name="arquivo_lista" 
                                                           value="<%= typeof indicacao !== 'undefined' && indicacao && indicacao.arquivo_lista ? indicacao.arquivo_lista : '' %>">
                                                </div>
                                            </div>

                                            <div class="row">
                                                <div class="col-md-12 mb-3">
                                                    <label for="observacoes" class="form-label">Observações</label>
                                                    <textarea class="form-control" id="observacoes" name="observacoes" rows="4"><%= typeof indicacao !== 'undefined' && indicacao && indicacao.observacoes ? indicacao.observacoes : '' %></textarea>
                                                </div>
                                            </div>

                                            <div class="d-flex gap-2">
                                                <button type="submit" class="btn btn-save">💾</button>
                                                <a href="/indicacoes" class="btn btn-clear" title="Limpar Campos">🧹</a>
                                            </div>
                                        </form>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </main>
            </nav>
        </div>
    </div>

    <!-- Rodapé -->
    <footer class="footer-custom">
        <div class="container-fluid">
            <div class="row">
                <div class="col-md-6">
                    <p class="mb-0">Total de Jurados: <span id="total-jurados">-</span></p>
                </div>
                <div class="col-md-6 text-md-end">
                    <p class="mb-0">Total de Instituições: <span id="total-instituicoes">-</span></p>
                </div>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Fetch total de jurados e instituições para o rodapé
            fetch('/api/stats')
                .then(response => response.json())
                .then(data => {
                    document.getElementById('total-jurados').textContent = data.totalJurados;
                    document.getElementById('total-instituicoes').textContent = data.totalInstituicoes;
                })
                .catch(error => console.error('Erro ao buscar estatísticas:', error));
        });
    </script>
</body>
</html>
