<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css?v=<%= Date.now() %>">
</head>
<body>
    <!-- Cabeçalho -->
    <header class="header-custom">
        <div class="container-fluid">
            <div class="d-flex justify-content-between align-items-center">
                <div class="d-flex align-items-center">
                    <img src="/images/logo-tjsc.png" alt="Logo TJSC" class="header-logo me-3">
                    <div>
                        <h1 class="header-title">Controle de Jurados</h1>
                        <p class="header-subtitle">Comarca de Capivari de Baixo</p>
                    </div>
                </div>
                <% if (typeof user !== 'undefined' && user) { %>
                <div class="dropdown">
                    <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-bs-toggle="dropdown" aria-expanded="false">
                        👤 <%= user.nome %>
                    </button>
                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="dropdownMenuButton">
                        <li><a class="dropdown-item" href="/perfil">⚙️ Perfil</a></li>
                        <% if (user.perfil === 'Administrador') { %>
                        <li><a class="dropdown-item" href="/usuarios">👥 Usuários</a></li>
                        <% } %>
                        <li><hr class="dropdown-divider"></li>
                        <li>
                            <form action="/login/logout" method="POST" class="d-inline">
                                <button type="submit" class="dropdown-item">🚪 Sair</button>
                            </form>
                        </li>
                    </ul>
                </div>
                <% } %>
            </div>
        </div>
    </header>

    <div class="main-container">
        <% if (typeof user !== 'undefined' && user) { %>
        <!-- Sidebar -->
        <nav class="sidebar">
            <div class="p-3">
                <ul class="nav flex-column">
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'dashboard' ? 'active' : '' %>" href="/dashboard">
                            📊 Dashboard
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'juizes' ? 'active' : '' %>" href="/juizes">
                            ⚖️ Juízes
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'instituicoes' ? 'active' : '' %>" href="/instituicoes">
                            🏢 Instituições
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'jurados' ? 'active' : '' %>" href="/jurados">
                            👥 Jurados
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'indicacoes' ? 'active' : '' %>" href="/indicacoes">
                            📋 Indicações
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'sorteios' ? 'active' : '' %>" href="/sorteios">
                            🎲 Sorteios
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'cedulas' ? 'active' : '' %>" href="/cedulas">
                            🎫 Cédulas
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'editais' ? 'active' : '' %>" href="/editais">
                            📄 Editais
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'emails' ? 'active' : '' %>" href="/emails">
                            📧 E-mails
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'relatorios' ? 'active' : '' %>" href="/relatorios">
                            📊 Relatórios
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'backup' ? 'active' : '' %>" href="/backup">
                            💾 Backup
                        </a>
                    </li>
                </ul>
            </div>
        </nav>
        <% } %>

        <!-- Conteúdo Principal -->
        <main class="main-content">
            <% if (typeof messages !== 'undefined' && messages) { %>
                <% if (messages.success && messages.success.length > 0) { %>
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    ✅ <%= messages.success[0] %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
                <% } %>
                <% if (messages.error && messages.error.length > 0) { %>
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    ❌ <%= messages.error[0] %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
                <% } %>
            <% } %>

            <% if (typeof error !== 'undefined' && error) { %>
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <i class="fas fa-exclamation-triangle btn-icon"></i><%= error %>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
            <% } %>

            <div class="container-fluid">
                <!-- Cabeçalho -->
                <div class="row mb-4">
                    <div class="col-12">
                        <div class="d-flex justify-content-between align-items-center">
                            <div>
                                <h2>Visualizar Indicação</h2>
                                <p class="text-muted mb-0">Detalhes da indicação</p>
                            </div>
                            <div>
                                <a href="/indicacoes/<%= indicacao.id %>/editar" class="btn btn-edit me-2" title="Editar">✏️</a>
                                <a href="/indicacoes" class="btn btn-back" title="Voltar">⬅️</a>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Conteúdo -->
                <div class="row">
                    <div class="col-lg-8">
                        <div class="card">
                            <div class="card-header">
                                <h5 class="card-title mb-0">Dados da Indicação</h5>
                            </div>
                            <div class="card-body">
                                <div class="row">
                                    <div class="col-md-6 mb-3">
                                        <label class="form-label fw-bold">Instituição</label>
                                        <p class="form-control-plaintext"><%= indicacao.instituicao_nome || 'N/A' %></p>
                                    </div>
                                    
                                    <div class="col-md-6 mb-3">
                                        <label class="form-label fw-bold">Ano de Referência</label>
                                        <p class="form-control-plaintext"><%= indicacao.ano_referencia %></p>
                                    </div>
                                </div>
                                
                                <div class="row">
                                    <div class="col-md-6 mb-3">
                                        <label class="form-label fw-bold">Quantidade de Cidadãos</label>
                                        <p class="form-control-plaintext"><%= indicacao.quantidade || 'N/A' %></p>
                                    </div>
                                    
                                    <div class="col-md-6 mb-3">
                                        <label class="form-label fw-bold">Prazo para Envio</label>
                                        <p class="form-control-plaintext"><%= indicacao.prazo_envio ? new Date(indicacao.prazo_envio).toLocaleDateString('pt-BR') : 'N/A' %></p>
                                    </div>
                                </div>
                                
                                <div class="row">
                                    <div class="col-md-6 mb-3">
                                        <label class="form-label fw-bold">Status</label>
                                        <p class="form-control-plaintext">
                                            <% 
                                            let badgeClass = 'bg-secondary';
                                            if (indicacao.status === 'pendente') badgeClass = 'bg-warning';
                                            else if (indicacao.status === 'recebida') badgeClass = 'bg-success';
                                            else if (indicacao.status === 'validada') badgeClass = 'bg-info';
                                            %>
                                            <span class="badge <%= badgeClass %>">
                                                <%= indicacao.status %>
                                            </span>
                                        </p>
                                    </div>
                                    
                                    <div class="col-md-6 mb-3">
                                        <label class="form-label fw-bold">Data de Solicitação</label>
                                        <p class="form-control-plaintext"><%= indicacao.created_at ? new Date(indicacao.created_at).toLocaleDateString('pt-BR') : 'N/A' %></p>
                                    </div>
                                </div>
                                
                                <div class="mb-3">
                                    <label class="form-label fw-bold">Arquivo PDF</label>
                                    <p class="form-control-plaintext">
                                        <% if (indicacao.arquivo_lista) { %>
                                            <a href="/uploads/<%= indicacao.arquivo_lista %>" target="_blank" class="text-decoration-none">
                                                📄 <%= indicacao.arquivo_lista %>
                                            </a>
                                        <% } else { %>
                                            Nenhum arquivo anexado
                                        <% } %>
                                    </p>
                                </div>
                                
                                <div class="mb-3">
                                    <label class="form-label fw-bold">Observações</label>
                                    <p class="form-control-plaintext"><%= indicacao.observacoes || 'Nenhuma observação' %></p>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Painel Lateral -->
                    <div class="col-lg-4">
                        <div class="card">
                            <div class="card-header">
                                <h5 class="card-title mb-0">Informações</h5>
                            </div>
                            <div class="card-body">
                                <div class="mb-3">
                                    <h6>📋 Sobre Esta Indicação</h6>
                                    <p class="text-muted small mb-0">
                                        Esta indicação foi criada para solicitar à instituição uma lista de cidadãos exemplares.
                                    </p>
                                </div>
                                
                                <div class="mb-3">
                                    <h6>📅 Status Atual</h6>
                                    <p class="text-muted small mb-0">
                                        <% if (indicacao.status === 'pendente') { %>
                                            Aguardando envio da lista pela instituição.
                                        <% } else if (indicacao.status === 'recebida') { %>
                                            Lista recebida e aguardando validação.
                                        <% } else if (indicacao.status === 'validada') { %>
                                            Lista validada e pronta para uso.
                                        <% } else { %>
                                            Status não definido.
                                        <% } %>
                                    </p>
                                </div>
                                
                                <div class="mb-3">
                                    <h6>📄 Arquivo Anexo</h6>
                                    <p class="text-muted small mb-0">
                                        <% if (indicacao.arquivo_lista) { %>
                                            Documento relacionado está disponível para download.
                                        <% } else { %>
                                            Nenhum documento foi anexado a esta indicação.
                                        <% } %>
                                    </p>
                                </div>
                                
                                <div class="alert alert-info">
                                    <small>
                                        <strong>💡 Dica:</strong> Você pode editar esta indicação clicando no botão "Editar" acima.
                                    </small>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Card de Ações Rápidas -->
                        <div class="card mt-3">
                            <div class="card-header">
                                <h5 class="card-title mb-0">Ações Rápidas</h5>
                            </div>
                            <div class="card-body">
                                <div class="d-grid gap-2">
                                    <a href="/indicacoes/<%= indicacao.id %>/editar" class="btn btn-warning">
                                        ✏️
                                    </a>
                                    <% if (indicacao.arquivo_lista) { %>
                                    <a href="/uploads/<%= indicacao.arquivo_lista %>" target="_blank" class="btn btn-primary">
                                        📄 Visualizar PDF
                                    </a>
                                    <% } %>
                                    <a href="/indicacoes" class="btn btn-secondary">
                                        ⬅️ Voltar
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </main>
    </div>

    <!-- Rodapé -->
    <footer class="footer-custom mt-5">
        <div class="container-fluid">
            <div class="row">
                <div class="col-md-6">
                    <p class="mb-0">© 2025 Tribunal de Justiça de Santa Catarina - TJSC</p>
                </div>
                <div class="col-md-6 text-md-end">
                    <p class="mb-0">Sistema de Controle de Jurados - Comarca de Capivari de Baixo</p>
                </div>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>