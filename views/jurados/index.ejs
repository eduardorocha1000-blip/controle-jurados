<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css?v=<%= Date.now() %>">
</head>
<body>
    <!-- Cabeçalho -->
    <header class="header-custom">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-auto">
                    <img src="/images/logo-tjsc.png" alt="Logo TJSC" class="header-logo">
                </div>
                <div class="col">
                    <h1 class="header-title">VARA ÚNICA DA COMARCA DE CAPIVARI DE BAIXO</h1>
                    <p class="header-subtitle">by Eduardo Motta Rocha da Silva</p>
                </div>
                <div class="col-auto">
                    <% if (typeof user !== 'undefined' && user) { %>
                    <div class="dropdown">
                        <button class="btn btn-outline-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown">
                            👤 <%= user.nome %>
                        </button>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="/perfil">⚙️ Perfil</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <form method="POST" action="/login/logout" class="d-inline">
                                    <button type="submit" class="dropdown-item">🚪 Sair</button>
                                </form>
                            </li>
                        </ul>
                    </div>
                    <% } %>
                </div>
            </div>
        </div>
    </header>

    <div class="main-container">
        <% if (typeof user !== 'undefined' && user) { %>
        <!-- Sidebar -->
        <nav class="sidebar">
            <div class="p-3">
                <ul class="nav flex-column">
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'dashboard' ? 'active' : '' %>" href="/dashboard">
                            📊 Dashboard
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'juizes' ? 'active' : '' %>" href="/juizes">
                            ⚖️ Juízes
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'instituicoes' ? 'active' : '' %>" href="/instituicoes">
                            🏢 Instituições
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'jurados' ? 'active' : '' %>" href="/jurados">
                            👥 Jurados
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'indicacoes' ? 'active' : '' %>" href="/indicacoes">
                            📋 Indicações
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'sorteios' ? 'active' : '' %>" href="/sorteios">
                            🎲 Sorteios
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/ultimo-conselho">🙋 Último Conselho</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'cedulas' ? 'active' : '' %>" href="/cedulas">
                            🎫 Cédulas
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'editais' ? 'active' : '' %>" href="/editais">
                            📄 Editais
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'emails' ? 'active' : '' %>" href="/emails">
                            📧 E-mails
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'relatorios' ? 'active' : '' %>" href="/relatorios">
                            📈 Relatórios
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'backup' ? 'active' : '' %>" href="/backup">💾 Backup</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'usuarios' ? 'active' : '' %>" href="/usuarios"><i class="fas fa-user-cog btn-icon"></i>Usuários</a>
                    </li>
                </ul>
            </div>
        </nav>
        <% } %>

        <!-- Conteúdo Principal -->
        <main class="main-content">
            <% if (typeof messages !== 'undefined' && messages) { %>
                <% if (messages.success && messages.success.length > 0) { %>
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    ✅ <%= messages.success[0] %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
                <% } %>
                <% if (messages.error && messages.error.length > 0) { %>
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    ❌ <%= messages.error[0] %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
                <% } %>
            <% } %>

            <div class="container-fluid">
                <!-- Cabeçalho -->
                <div class="row mb-4">
                    <div class="col-12">
                        <div class="d-flex justify-content-between align-items-center">
                            <div>
                                <h2>Jurados</h2>
                            </div>
                            <div>
                                <a href="/jurados/novo" class="btn btn-primary">
                                    ➕ Novo Jurado
                                </a>
                                <form method="POST" action="/jurados/reativar-suspensos" class="d-inline ms-2">
                                    <button type="submit" class="btn btn-warning">
                                        🔄 Reativar Suspensos
                                    </button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Estatísticas -->
                <div class="row mb-4 stats-cards">
                    <div class="col-md-3 mb-3">
                        <div class="card h-100">
                            <div class="card-body">
                                <div class="d-flex align-items-center">
                                    <div class="flex-shrink-0">
                                        <div class="text-primary fs-5">👥</div>
                                    </div>
                                    <div class="flex-grow-1 ms-3">
                                        <h5 class="card-title mb-0">Total</h5>
                                        <h3 class="mb-0"><%= typeof totalJurados !== 'undefined' ? totalJurados : 0 %></h3>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <div class="col-md-3 mb-3">
                        <div class="card h-100">
                            <div class="card-body">
                                <div class="d-flex align-items-center">
                                    <div class="flex-shrink-0">
                                        <div class="text-success fs-5">🟢</div>
                                    </div>
                                    <div class="flex-grow-1 ms-3">
                                        <h5 class="card-title mb-0">Ativos</h5>
                                        <h3 class="mb-0"><%= typeof juradosAtivos !== 'undefined' ? juradosAtivos : 0 %></h3>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <div class="col-md-3 mb-3">
                        <div class="card h-100">
                            <div class="card-body">
                                <div class="d-flex align-items-center">
                                    <div class="flex-shrink-0">
                                        <div class="text-danger fs-5">🔴</div>
                                    </div>
                                    <div class="flex-grow-1 ms-3">
                                        <h5 class="card-title mb-0">Inativos</h5>
                                        <h3 class="mb-0"><%= typeof juradosInativos !== 'undefined' ? juradosInativos : 0 %></h3>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <div class="col-md-3 mb-3">
                        <div class="card h-100">
                            <div class="card-body">
                                <div class="d-flex align-items-center">
                                    <div class="flex-shrink-0">
                                        <div class="text-info fs-5">📅</div>
                                    </div>
                                    <div class="flex-grow-1 ms-3">
                                        <h5 class="card-title mb-0">Este Ano</h5>
                                        <h3 class="mb-0"><%= typeof juradosAno !== 'undefined' ? juradosAno : 0 %></h3>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Filtros -->
                <div class="card mb-4">
                    <div class="card-body">
                        <form method="GET" action="/jurados" class="row g-3">
                            <div class="col-md-5">
                                <label for="busca" class="form-label">Buscar por Nome ou CPF</label>
                                <input type="text" class="form-control" id="busca" name="busca" placeholder="Digite o nome ou CPF..." value="<%= typeof filtros !== 'undefined' && filtros.busca ? filtros.busca : '' %>">
                            </div>
                            <div class="col-md-3">
                                <label for="instituicao_id" class="form-label">Instituição</label>
                                <select class="form-select" id="instituicao_id" name="instituicao_id">
                                    <option value="">Todas as instituições</option>
                                    <% if (typeof instituicoes !== 'undefined' && instituicoes.length > 0) { %>
                                        <% for (let i = 0; i < instituicoes.length; i++) { %>
                                            <% const instituicao = instituicoes[i]; %>
                                            <option value="<%= instituicao.id %>" <%= typeof filtros !== 'undefined' && filtros.instituicao_id == instituicao.id ? 'selected' : '' %>>
                                                <%= instituicao.nome %>
                                            </option>
                                        <% } %>
                                    <% } %>
                                </select>
                            </div>
                            <div class="col-md-2">
                                <label for="status" class="form-label">Status</label>
                                <select class="form-select" id="status" name="status">
                                    <option value="">Todos os status</option>
                                    <option value="Ativo" <%= typeof filtros !== 'undefined' && filtros.status === 'Ativo' ? 'selected' : '' %>>Ativo</option>
                                    <option value="Inativo" <%= typeof filtros !== 'undefined' && filtros.status === 'Inativo' ? 'selected' : '' %>>Inativo</option>
                                </select>
                            </div>
                            <div class="col-md-2 d-flex align-items-end">
                                <div class="d-flex gap-2 w-100">
                                    <button type="submit" class="btn btn-filter" title="Filtrar">🔍</button>
                                    <button type="button" class="btn btn-clear" onclick="limparFiltros()" title="Limpar">🧹</button>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>

                <!-- Tabela de Jurados -->
                <div class="card">
                    <div class="card-body">
                        <% if (typeof jurados !== 'undefined' && jurados.length > 0) { %>
                        <div class="table-responsive">
                            <table class="table table-hover">
                                <thead>
                                    <tr>
                                        <th>Nome</th>
                                        <th>CPF</th>
                                        <th>Idade</th>
                                        <th>Telefone</th>
                                        <th>E-mail</th>
                                        <th class="text-center">Status</th>
                                        <th>Instituição</th>
                                        <th>Ações</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% for (let i = 0; i < jurados.length; i++) { %>
                                    <% const jurado = jurados[i]; %>
                                    <tr>
                                        <td><%= jurado.nome_completo %></td>
                                        <td><%= jurado.cpf %></td>
                                        <td>
                                            <% if (jurado.data_nascimento) { %>
                                                <% 
                                                const hoje = new Date();
                                                const nascimento = new Date(jurado.data_nascimento);
                                                const idade = hoje.getFullYear() - nascimento.getFullYear();
                                                %>
                                                <%= idade %> anos
                                            <% } else { %>
                                                -
                                            <% } %>
                                        </td>
                                        <td><%= jurado.telefone || '-' %></td>
                                        <td><%= jurado.email || '-' %></td>
                                        <td class="text-center">
                                            <span class="badge <%= jurado.status === 'Ativo' ? 'bg-success' : 'bg-danger' %>">
                                                <%= jurado.status %>
                                            </span>
                                        </td>
                                        <td><%= jurado.instituicao_nome || '-' %></td>
                                        <td>
                                            <div class="btn-group btn-group-sm" role="group">
                                                <a href="/jurados/<%= jurado.id %>" class="btn btn-view btn-sm" title="Visualizar">👁️</a>
                                                <a href="/jurados/<%= jurado.id %>/editar" class="btn btn-edit btn-sm" title="Editar">✏️</a>
                                                <form method="POST" action="/jurados/<%= jurado.id %>?_method=DELETE" class="d-inline">
                                                    <button type="submit" class="btn btn-delete btn-sm" title="Excluir">🗑️</button>
                                                </form>
                                            </div>
                                        </td>
                                    </tr>
                                    <% } %>
                                </tbody>
                            </table>
                        </div>
                        <% } else { %>
                        <div class="text-center py-5">
                            <div class="text-muted mb-3" style="font-size: 3rem;">👥</div>
                            <h5 class="text-muted">Nenhum jurado cadastrado</h5>
                            <p class="text-muted">Comece adicionando o primeiro jurado ao sistema.</p>
                            <a href="/jurados/novo" class="btn btn-primary">
                                ➕ Adicionar Primeiro Jurado
                            </a>
                        </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </main>
    </div>

    <!-- Rodapé -->
    <footer class="footer-custom">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <p class="mb-0">Total de Jurados: <span id="total-jurados">-</span></p>
                </div>
                <div class="col-md-6 text-md-end">
                    <p class="mb-0">Total de Instituições: <span id="total-instituicoes">-</span></p>
                </div>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/table-sort.js"></script>
    <script>
        // Função para limpar filtros
        function limparFiltros() {
            document.querySelector('input[name="busca"]').value = '';
            document.querySelector('select[name="status"]').value = '';
            document.querySelector('select[name="instituicao_id"]').value = '';
            // Redirecionar para a página sem filtros
            window.location.href = '/jurados';
        }

        function voltarPagina() {
            if (window.history.length > 1) {
                window.history.back();
            } else if (document.referrer) {
                window.location.href = document.referrer;
            } else {
                window.location.href = '/jurados';
            }
        }

        // Carregar totais no rodapé
        fetch('/api/stats')
            .then(response => response.json())
            .then(data => {
                document.getElementById('total-jurados').textContent = data.totalJurados || 0;
                document.getElementById('total-instituicoes').textContent = data.totalInstituicoes || 0;
            })
            .catch(error => console.error('Erro ao carregar estatísticas:', error));
    </script>
</body>
</html>
